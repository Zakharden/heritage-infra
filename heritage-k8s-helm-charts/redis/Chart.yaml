apiVersion: v2
name: heritage-redis
description: Wrapper chart for single-node Redis
type: application
version: 0.1.0
appVersion: "7"
dependencies:
  - name: redis
    # Pinned + vendored to avoid Helm repo/index access from Argo CD repo-server.
    version: "20.13.4"
    repository: "file://_vendor/redis"
