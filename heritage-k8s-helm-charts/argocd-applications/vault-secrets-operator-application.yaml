apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: heritage-vault-secrets-operator
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: default
  source:
    repoURL: git@github.com:Zakharden/heritage-infra.git
    targetRevision: HEAD
    path: heritage-k8s-helm-charts/vault-secrets-operator
    helm:
      releaseName: heritage-vault-secrets-operator
  destination:
    server: https://kubernetes.default.svc
    namespace: vault-secrets-operator-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
