apiVersion: v2
name: heritage-postgres
description: Wrapper chart for PostgreSQL primary+replica topology
type: application
version: 0.1.0
appVersion: "16"
dependencies:
  - name: postgresql
    # Pinned + vendored to avoid Helm repo/index access from Argo CD repo-server.
    version: "16.7.27"
    repository: "file://_vendor/postgresql"
